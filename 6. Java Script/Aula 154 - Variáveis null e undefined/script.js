// Null está apontando para o vázio
var nome = null;

// undefined está falando que não tem nada dentro
var idade = undefined;
// se não declarar nada, o valor padrão já é undefined
var idade2;

console.log(nome, idade, idade2);
